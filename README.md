# SWE30003-Assigment-3


We should create different branches for better version control.
git checkout -b <your name/ Testing Branch>

For example
git checkout -b MKHA/Main

After all these commits, we can merge them when the branches are ready.
